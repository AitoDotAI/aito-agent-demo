import AppShell from "@/components/AppShell";

// "/sales/" opens the same shell on the Northlight Opportunity Assistant tab.
export default function Sales() {
  return <AppShell initialView="sales" />;
}
